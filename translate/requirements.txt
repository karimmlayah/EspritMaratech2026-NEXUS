# --- translate (voice-to-sign, serveur, mediapipe) ---
flask>=2.0
flask-cors>=3.0
numpy>=1.20
torch>=2.0
opencv-python>=4.5
mediapipe>=0.10.0
# Vidéo → sous-titres : extraction audio + speech-to-text (ffmpeg doit être installé sur le système)
openai-whisper>=20231117

# --- Arabic Sign-to-Text (Arabic-Sign-Language-main) ---
arabic-reshaper>=3.0.0
# dlib: optionnel (landmarks visage). Sous Windows: lancer translate\install_dlib_windows.bat pour une wheel precompilee (sans Visual Studio).
Pillow>=8.1.0
python-bidi>=0.4.2
scipy>=1.6.0
tensorflow>=2.15
imutils>=0.5.4
